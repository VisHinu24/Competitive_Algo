ll mod = 1e9 + 7;
ll mul(ll a,ll b){
    return (a*1ll*b)%mod;
}
ll divide (ll a,ll b){
    return a * ((ll)pow(b,mod-2)%mod);
}
ll power(ll a,ll b){
    ll ans = 1;
    while(b>0){
        if (b&1) ans = mul(ans,a);
        a=mul(a,a);
        b=b>>1; 
    }
    return ans;
}
